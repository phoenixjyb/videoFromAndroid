# Unified Orin API Requirements
# Python dependencies for both Target API and Media API
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data validation
pydantic>=2.4.0

# File upload support (for Media API)
python-multipart>=0.0.6

# HTTP client (for testing)
httpx>=0.25.0
requests>=2.31.0

# Note: ROS2 dependencies are installed via system packages
# sudo apt install ros-humble-rclpy ros-humble-sensor-msgs
